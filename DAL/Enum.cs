using System;

namespace IDAL
{
    namespace DO
    {
        public enum WeightCategories { iow, middle, heavy};
        public enum DronStatuses { free, maintenance, shipping};
        public enum Priorities { regular, fast, emergency};
    }
}
